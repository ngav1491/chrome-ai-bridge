name: CI

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Lint, typecheck, build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        # --ignore-scripts prevents the native-server postinstall hook from
        # running before its own dist/ has been built.
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build the workspaces that ship a dist (needed by postinstall)
        run: pnpm build:shared && pnpm build:native

      - name: Re-run postinstall hooks with dist/ in place
        run: pnpm rebuild

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        # The chrome-extension workspace is built by Vite + WXT (which already
        # runs vue-tsc). Running plain `tsc --noEmit` there fails because the
        # extension's tsconfig is not set up for type-checking every transitive
        # type definition. Restrict typecheck to the Node-side workspaces.
        run: |
          pnpm --filter chrome-mcp-shared exec tsc --noEmit
          pnpm --filter chrome-ai-bridge exec tsc --noEmit

      - name: Build all workspaces
        run: pnpm build
