#!/usr/bin/env node import path from 'path'; import { COMMAND_NAME } from './constant'; import { colorText, registerWithElevatedPermissions, writeNodePathFile } from './utils'; /** * hàm */ async function main(): Promise<void> { console.log(colorText(`đangđăng ký ${COMMAND_NAME} Native Messaginghost..`, 'blue')); try { // Write Node.js path before registration writeNodePathFile(path.join(__dirname, '.')); await registerWithElevatedPermissions(); console.log(colorText('đăng kýthành công! Chrome thông quaNative Messaging cục bộdịch vụ .', 'green',),); } catch (error: any) { console.error(colorText(`đăng kýthất bại: ${error.message}`, 'red')); process.exit(1); } } // thực thihàm main();
